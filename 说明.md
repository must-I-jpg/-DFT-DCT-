这个脚本基本是按

任务：在图像频域中嵌入不可见的数字水印，并能够成功提取出来
基础：对图像进行DFT等变换，在频域特定位置嵌入水印信息，通过逆变换获得含水印图像；验证水印的不可见性（人眼难以察觉），并实现从含水印图像中正确提取出原始水印
进阶：分析嵌入强度对图像质量的影响；测试水印的“抗打击能力”（如对图像进行JPEG压缩、缩放、加噪后，还能否提取出水印）
挑战：尝试在DCT域或DWT域嵌入；设计“盲水印”算法（即提取水印时不需要原始原图参与）

完整组织的。主线是 **DFT 非盲水印**，后面再做强度分析、攻击测试，以及 DCT/DWT/盲水印对比。

**1. 输入与水印预处理**

脚本先读取：

- `input.png`：原始载体图像
- `watermark.png`：水印图像

对应函数是 `load_inputs()` 和 `prepare_watermark()`。

`prepare_watermark()` 会把水印缩放成适合载体图像大小的正方形，然后二值化：

```python
_, wm_bin = cv2.threshold(wm, 127, 255, cv2.THRESH_BINARY)
wm_pm1 = np.where(wm_bin > 0, 1.0, -1.0)
```

也就是说，水印被转换成两种形式：

- `wm_bin`：0/255 的二值水印，用于显示和评价
- `wm_pm1`：-1/+1 的水印信号，用于嵌入 DFT 频谱系数

**2. 基础任务：DFT 频域嵌入不可见水印**

核心函数是 `dft_embed_channel()`。

它对图像每个颜色通道做二维 DFT：

```python
f = np.fft.fftshift(np.fft.fft2(channel))
```

然后在频谱中心附近选择一块区域嵌入水印。位置由 `dft_embed_position()` 决定：

```python
y = cy - wh // 2
x = cx + max(1, int(min(h, w) * offset_ratio // 4))
```

这表示嵌入区域在 **移频后的频谱中心附近，略偏右**，属于中低频区域。脚本还会计算一个中心对称区域：

```python
sy = 2 * cy - y - wh
sx = 2 * cx - x - ww
```

嵌入公式是：

```python
f[region] = coeff + alpha * scale * wm_pm1 * phase
```

含义是：在原 DFT 系数 `coeff` 的相位方向上，按强度 `alpha` 加入水印信号。这样主要修改频谱系数幅度，尽量减少对图像视觉结构的破坏。

同时，脚本还在对称频谱区域嵌入翻转水印：

```python
f[sym_region] = coeff_sym + alpha * scale_sym * np.flipud(np.fliplr(wm_pm1)) * phase_sym
```

这样做是为了保持频谱的对称性，使逆 DFT 后得到的空间域图像仍然稳定、接近实值图像。

最后做逆变换得到含水印图像：

```python
out = np.real(np.fft.ifft2(np.fft.ifftshift(f)))
```

**3. 基础任务：从含水印图像中提取水印**

提取函数是 `dft_extract_channel()` 和 `dft_extract()`。

因为这是 **非盲水印算法**，所以提取时需要原图参与。脚本分别计算原图和含水印图像的 DFT：

```python
f0 = np.fft.fftshift(np.fft.fft2(original))
f1 = np.fft.fftshift(np.fft.fft2(watermarked))
```

然后用两者频谱差值恢复水印：

```python
ext = np.real((f1[region] - coeff) * np.conj(phase)) / (alpha * scale + 1e-12)
```

简单说就是：

> 含水印频谱 - 原始频谱 = 嵌入进去的水印扰动

最后把三个颜色通道提取出的结果投票融合：

```python
return np.where(np.sum(extracted, axis=0) > 255 * 1.5, 255, 0).astype(np.uint8)
```

**4. 不可见性验证**

脚本用两个指标评价含水印图像和原图是否接近：

```python
quality_metrics()
```

里面计算：

- `PSNR`：峰值信噪比，越高说明失真越小
- `SSIM`：结构相似度，越接近 1 说明视觉结构越相似

同时 `save_basic_result()` 会生成 `basic_dft_result.png`，展示：

- 原始载体图像
- 含水印图像
- 差异放大图
- 原始水印
- 提取水印
- PSNR、SSIM、NC 指标

其中 `NC` 是归一化相关系数，用于衡量提取水印和原始水印是否一致。

脚本还会生成 `frequency_comparison.png`，用于直观展示水印实际嵌入到 DFT 频域中的位置。该图按 B、G、R 三个颜色通道分为三行，每行包含：

- DFT 嵌入前幅度谱
- DFT 嵌入后幅度谱
- 嵌入前后的频域变化差异

图中的 `main` 表示主嵌入区域，`sym` 表示中心对称嵌入区域。右侧差异图中出现的水印形状，说明脚本修改的是频谱系数，而不是直接把水印画到空间域图像上。

**5. 进阶任务：分析嵌入强度 alpha 的影响**

函数是 `alpha_scan()`。

它遍历多个 `alpha`：

```python
alphas = np.linspace(0.01, 0.3, 15)
```

每个 alpha 都重新嵌入、提取，然后计算：

- PSNR
- SSIM
- NC

并保存为：

```text
alpha_scan.png
```

这张图可以用来分析：

- `alpha` 小：图像质量好，但水印可能不稳定
- `alpha` 大：水印更容易提取，但图像失真增加
- 需要在不可见性和鲁棒性之间折中

**6. 进阶任务：抗打击能力测试**

函数是 `robustness_tests()`。

它对含水印图像做三类攻击：

```python
"JPEG 压缩": [90, 70, 50, 30, 20, 10]
"缩放": [0.5, 0.75, 1.25, 1.5, 2.0]
"高斯噪声": [5, 10, 15, 20, 30, 50]
```

攻击后再尝试提取水印：

```python
ext = dft_extract(cover, attacked_float, wm_shape, pos, ALPHA)
```

然后计算提取水印和原始水印的 `NC`。结果保存为：

```text
robustness_summary.png
```

这部分就是验证水印在 JPEG 压缩、缩放、加噪后的可恢复程度。

**7. 挑战任务：DCT 域水印**

脚本实现了 `dct_embed()` 和 `dct_extract()`。

它先把图像转成 `YCrCb`，只在亮度通道 `Y` 上嵌入：

```python
y = ycrcb[:, :, 0].astype(np.float64) / 255.0
```

然后按 `8x8` 分块做 DCT：

```python
block = cv2.dct(work[y:y + 8, x:x + 8])
```

在每个块的中频系数位置嵌入比特：

```python
positions = [(3, 3), (3, 4), (4, 3), (4, 4)]
```

采用的是 QIM 思想：通过量化系数的奇偶性表示 0 或 1。

**8. 挑战任务：DWT 域水印**

脚本实现了简化 Haar 小波变换：

```python
haar_dwt2()
haar_idwt2()
```

`dwt_experiment()` 中先把图像转为亮度通道，然后分解成：

- `LL`：低频近似部分
- `LH`
- `HL`
- `HH`

脚本把水印嵌入到 `LH` 和 `HL` 两个细节子带：

```python
lh_wm = lh + alpha * (np.std(lh) + 1e-8) * wm
hl_wm = hl + alpha * (np.std(hl) + 1e-8) * wm
```

这属于 DWT 域加性水印。

**9. 挑战任务：盲水印算法**

脚本实现了 `blind_dft_embed()` 和 `blind_dft_extract()`。

它仍然使用 DFT 频域，但不再用“含水印频谱 - 原始频谱”来提取，而是通过 QIM 量化系数幅值：

```python
q = int(np.floor(abs(coeff) / delta + 0.5))
if q % 2 != int(bits[i, j]):
    q += 1
f[oy + i, ox + j] = q * delta * np.exp(1j * np.angle(coeff))
```

提取时只看含水印图像的 DFT 系数量化结果的奇偶性：

```python
main[i, j] = int(np.floor(abs(f[y + i, x + j]) / delta + 0.5)) % 2
```

所以这个方法提取时 **不需要原始图像**，属于盲水印。

**10. 方法对比与输出**

`compare_methods()` 会统一比较四种方法：

- `DFT 非盲`
- `DCT QIM`
- `DWT`
- `DFT 盲 QIM`

比较指标包括：

- PSNR
- SSIM
- NC

并生成：

```text
method_comparison.png
```

主函数 `main()` 的完整流程是：

```python
读取图像和水印
准备二值水印
计算 DFT 嵌入位置
执行 DFT 非盲水印嵌入
提取 DFT 水印
计算 PSNR、SSIM、NC
保存基础结果图
保存频谱差异图
保存 B/G/R 三通道 DFT 频谱对比图
扫描 alpha 强度
做 JPEG/缩放/加噪鲁棒性测试
比较 DCT、DWT、DFT 盲 QIM 方法
打印实验报告
```

总结一句话：

> 这个脚本以 DFT 非盲频域水印为主算法，完成了水印嵌入、逆变换生成含水印图像、基于原图的水印提取、不可见性评价、嵌入强度分析、三通道频谱可视化、鲁棒性攻击测试，并额外实现了 DCT、DWT 和 DFT 盲 QIM 三种扩展方法用于挑战部分对比。
>
